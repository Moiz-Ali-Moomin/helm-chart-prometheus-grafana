apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grafana-chart.fullname" . }}-test-connection"
  labels:
    {{- include "grafana-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grafana-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
